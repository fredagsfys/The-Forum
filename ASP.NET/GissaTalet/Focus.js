var validatorFocus = {

    init: function (e) {
        var myGuessButton = document.getElementById('myGuessButton').focus();

        myGuessButton = function() {
            var lol = document.getElementById('generateNewButton').focus();
            }
        }
    }


};
window.onload = validatorFocus.init;